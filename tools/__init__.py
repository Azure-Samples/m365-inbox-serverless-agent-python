"""Local @tool helpers for the inbox agent sample.

Everything that needs Outlook or Teams is provided by the MCP connectors
declared in `mcp.json`. This package only contains pure-Python helpers that
agents can call alongside MCP tools.
"""
